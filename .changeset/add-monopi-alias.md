---
monopi-group: minor
---

# Unscoped npm alias package

Add the unscoped `monopi` npm package as a thin alias that re-exports the `@monopi/monopi` binary, so `npx monopi` works as an alternative to `npx @monopi/monopi`.
