---
monopi: patch
---

# Fix Ollama Cloud model metadata

Ollama Cloud model metadata now applies authoritative context-window and reasoning defaults for known cloud models, including GLM 5.2's 1,000,000-token context window and restored thinking levels for stale cached or credential-stored model entries.
