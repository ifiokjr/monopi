---
monopi: patch
---

# Remove bg-process compatibility package

The unpublished `@monopi/extension-bg-process` compatibility package has been removed so only `@monopi/background-tasks` provides the `bg_task` and `bg_status` tools.
