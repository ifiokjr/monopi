---
monopi: patch
---

# Update workspace dependencies

Direct workspace dependencies are updated to the latest published versions, pi peer minimums now require the secure 0.80.3 baseline, and security overrides are refreshed for patched transitive releases.
