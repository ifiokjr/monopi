---
monopi: patch
---

# Upgrade to pi 0.84.1 and migrate provider auth

Update every package's pi peer requirements and move provider credentials and model refreshes to the current public registry APIs.
