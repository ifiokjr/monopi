---
monopi-group: patch
---

# Refresh project documentation

- Remove the last ant-colony reference from the changelog (nest lock recovery entry).
- Restore the missing subagents heading in the feature catalog and drop stale plan-mode and `/spec` sections that no longer exist in the codebase.
- Update the split-extension inventory, built-in subagent templates, AGENTS.md template count, CLI presets, Node.js/pnpm baselines, and MonoChange command references in README and guide pages.
- Link the agent-workflow guides from the docs index and fix subagent examples to use current built-in agents.